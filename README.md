# Portfolio

Personal portfolio site built with Vite + React + TypeScript.

## Get started

```bash
npm install
npm run dev
```

Open the local URL Vite prints (usually `http://localhost:5173`).

## Customize

Edit `src/content.ts` with your name, bio, projects, email, and links.
